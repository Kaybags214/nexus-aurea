# Learnings — awb-review

Append after every run. Newest at the top.

Format:

```
## YYYY-MM-DD — <AWB no. / lane>
- What the document did that the skill did not expect:
- What the skill got wrong or missed:
- Change to make to SKILL.md:
```

---

## Seeded expectations — replace with real observations

- The check-digit test is deterministic and cheap; it should catch transcription errors that a
  visual read misses. Track how often it actually fires — if it never does, move it later in
  the order so the expensive cross-document work runs first.
- Unclear whether the shipments reviewed here are mostly MAWB or HAWB. If house waybills
  dominate, this skill needs a section on the house/master reference pair.
