# Intake

Drop redacted practice document scans here for auditing. **Do not commit real customer documents
with confidential data** — redact names/addresses/values first (matches the `audit-lab/` rule).

Finished audits are written to `../audit-reports/`.
